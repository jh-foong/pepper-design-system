import 'package:flutter/material.dart';

class AppShadows {
  static const focusRingsDefault = BoxShadow(
    color: Color(0xff0165fa),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 4,
  );

  static const focusRingsDefault2 = BoxShadow(
    color: Color(0xffffffff),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const focusRingsInverse = BoxShadow(
    color: Color(0xffffffff),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 4,
  );

  static const focusRingsInverse2 = BoxShadow(
    color: Color(0xff0a0a0a),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const focusRingsError = BoxShadow(
    color: Color(0xffc10007),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 4,
  );

  static const focusRingsError2 = BoxShadow(
    color: Color(0xffffffff),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const focusRingsSubtle = BoxShadow(
    color: Color(0xff0a0a0a),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 4,
  );

  static const focusRingsSubtle2 = BoxShadow(
    color: Color(0xffffffff),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const iconLightEffect = BoxShadow(
    color: Color(0x1a347ffc),
    offset: Offset(0, -3),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const iconLightEffect2 = BoxShadow(
    color: Color(0x1affffff),
    offset: Offset(2, 3),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const iconDarkEffect = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 4),
    blurRadius: 6,
    spreadRadius: -1,
  );

  static const iconDarkEffect2 = BoxShadow(
    color: Color(0x4dffffff),
    offset: Offset(-3, 3),
    blurRadius: 14,
    spreadRadius: 0,
  );

  static const iconDarkEffect3 = BoxShadow(
    color: Color(0x33000000),
    offset: Offset(15, -15),
    blurRadius: 27,
    spreadRadius: 0,
  );

  static const iconDarkEffect4 = BoxShadow(
    color: Color(0x33002768),
    offset: Offset(0, -1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const shadow2Xs = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 1),
    blurRadius: 1,
    spreadRadius: 0,
  );

  static const shadowXs = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0,
  );

  static const shadowSm = BoxShadow(
    color: Color(0x0d000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: -1,
  );

  static const shadowMd = BoxShadow(
    color: Color(0x1a000000),
    offset: Offset(0, 4),
    blurRadius: 6,
    spreadRadius: -1,
  );

  static const shadowLg = BoxShadow(
    color: Color(0x1a000000),
    offset: Offset(0, 10),
    blurRadius: 15,
    spreadRadius: -3,
  );

  static const shadowXl = BoxShadow(
    color: Color(0x1a000000),
    offset: Offset(0, 20),
    blurRadius: 25,
    spreadRadius: -5,
  );

  static const shadow2Xl = BoxShadow(
    color: Color(0x40000000),
    offset: Offset(0, 25),
    blurRadius: 50,
    spreadRadius: -12,
  );

  AppShadows._();
}
