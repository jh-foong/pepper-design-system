{
  "v": 0,
  "components": {
    "foundations-color": {
      "id": "foundations-color",
      "name": "Color",
      "path": "./src/stories/foundations/Color.stories.jsx",
      "jsDocTags": {},
      "error": {
        "name": "No component found",
        "message": "We could not detect the component from your story file. Specify meta.component.\n  290 | }\n  291 |\n> 292 | export default {\n      | ^\n  293 |   title: 'Foundations/Color',\n  294 |   tags: ['ai-generated'],\n  295 |   parameters: { layout: 'padded' },\n\n./src/stories/foundations/Color.stories.jsx:\nimport { useMemo, useState } from 'react';\nimport { listTokens, groupPrimitives } from '../../utils/tokens';\n\nconst CODE_FONT = \"'SF Mono', 'Roboto Mono', ui-monospace, monospace\";\n\nfunction Swatch({ name, value }) {\n  return (\n    <div style={{ display: 'flex', alignItems: 'center', gap: 12, padding: '6px 0' }}>\n      <div\n        style={{\n          width: 40,\n          height: 40,\n          borderRadius: 8,\n          border: '1px solid var(--pepper-color-fg-stroke, #ccc)',\n          background: `var(${name})`,\n          flexShrink: 0,\n        }}\n      />\n      <div style={{ fontFamily: 'monospace', fontSize: 12, lineHeight: 1.4 }}>\n        <div>{name}</div>\n        <div style={{ opacity: 0.6 }}>{value}</div>\n      </div>\n    </div>\n  );\n}\n\nfunction TokenGrid({ tokens }) {\n  return (\n    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(260px, 1fr))', gap: 4 }}>\n      {tokens.map((t) => (\n        <Swatch key={t.name} {...t} />\n      ))}\n    </div>\n  );\n}\n\n// Matches the curated family order on the \"Colors: Primitives\" page in Bell\n// (Figma node 40001040:1549) — anything not listed falls back to the end,\n// alphabetically, so a new family never disappears, just lands out of order.\nconst PRIMITIVE_FAMILY_ORDER = [\n  'brand-primary-blue',\n  'brand-crypto-orange',\n  'brand-secondary-cyan',\n  'midnight',\n  'neutral',\n  'system-green',\n  'system-orange',\n  'system-red',\n  'decorative-lime',\n  'decorative-magenta',\n  'decorative-turquoise',\n  'decorative-violet',\n  'decorative-yellow',\n];\n\nfunction orderFamilies(families) {\n  const known = PRIMITIVE_FAMILY_ORDER.filter((f) => families.has(f));\n  const rest = [...families.keys()].filter((f) => !PRIMITIVE_FAMILY_ORDER.includes(f)).sort();\n  return [...known, ...rest];\n}\n\nfunction familyDisplayName(family) {\n  return family.split('-').join(' / ');\n}\n\nfunction stepOf(name) {\n  const match = name.match(/-(\\d+|white|black)(?:-opacity)?$/);\n  return match ? match[1] : name;\n}\n\n// white sorts lighter than every numbered step, black sorts darker than all of them.\nfunction stepSortValue(step) {\n  if (step === 'white') return -1;\n  if (step === 'black') return Infinity;\n  return Number(step);\n}\n\n// Ramp presentation, styled after the Atlassian Design System's color\n// palette page (atlassian.design/foundations/color/color-palette) — a\n// continuous stack of full-width ramp bars per family, step name on top in\n// auto-contrast text. Click a bar to reveal Hex/RGB/Token copy actions;\n// hover any one for a \"Copy to clipboard\" tooltip, click to copy.\nfunction hexToRgb(hex) {\n  const c = hex.replace('#', '');\n  const r = parseInt(c.substring(0, 2), 16);\n  const g = parseInt(c.substring(2, 4), 16);\n  const b = parseInt(c.substring(4, 6), 16);\n  return `rgb(${r}, ${g}, ${b})`;\n}\n\nfunction contrastTextColor(hex) {\n  if (!hex?.startsWith('#') || hex.length < 7) return '#000';\n  const c = hex.replace('#', '');\n  const lin = (v) => (v <= 0.03928 ? v / 12.92 : ((v + 0.055) / 1.055) ** 2.4);\n  const [r, g, b] = [0, 2, 4].map((i) => lin(parseInt(c.substring(i, i + 2), 16) / 255));\n  const luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b;\n  return luminance > 0.4 ? '#000' : '#fff';\n}\n\nfunction CopyIcon({ color }) {\n  return (\n    <svg width=\"13\" height=\"13\" viewBox=\"0 0 16 16\" fill=\"none\" style={{ flexShrink: 0 }}>\n      <rect x=\"5.5\" y=\"5.5\" width=\"8\" height=\"8\" rx=\"1.5\" stroke={color} strokeWidth=\"1.2\" />\n      <path d=\"M3.5 10.5h-1a1 1 0 0 1-1-1v-7a1 1 0 0 1 1-1h7a1 1 0 0 1 1 1v1\" stroke={color} strokeWidth=\"1.2\" />\n    </svg>\n  );\n}\n\n// Custom tooltip (not the native `title` attribute) so it can switch its own\n// text to \"Copied\" right after the click, the way Atlassian's copy buttons do.\nfunction CopyLabel({ text, value, color }) {\n  const [copied, setCopied] = useState(false);\n  const [hovered, setHovered] = useState(false);\n\n  const handleClick = async (e) => {\n    e.stopPropagation();\n    try {\n      await navigator.clipboard.writeText(value);\n    } catch {\n      // clipboard unavailable (e.g. insecure context) — tooltip still told them what it does\n    }\n    setCopied(true);\n    setTimeout(() => setCopied(false), 1200);\n  };\n\n  return (\n    <span\n      onClick={handleClick}\n      onMouseEnter={() => setHovered(true)}\n      onMouseLeave={() => setHovered(false)}\n      style={{\n        position: 'relative',\n        display: 'inline-flex',\n        alignItems: 'center',\n        gap: 4,\n        textDecoration: 'underline',\n        cursor: 'pointer',\n      }}\n    >\n      {text}\n      {(hovered || copied) && (\n        <span\n          style={{\n            position: 'absolute',\n            bottom: '100%',\n            left: '50%',\n            transform: 'translateX(-50%)',\n            marginBottom: 6,\n            background: '#1a1a1a',\n            color: '#fff',\n            padding: '4px 8px',\n            borderRadius: 4,\n            fontSize: 11,\n            whiteSpace: 'nowrap',\n            pointerEvents: 'none',\n            zIndex: 10,\n          }}\n        >\n          {copied ? 'Copied' : `Copy '${value}' to clipboard`}\n        </span>\n      )}\n    </span>\n  );\n}\n\nfunction RampBar({ token, isFirst, isLast }) {\n  const [revealed, setRevealed] = useState(false);\n  const textColor = contrastTextColor(token.value);\n  return (\n    <div\n      role=\"button\"\n      tabIndex={0}\n      onClick={() => setRevealed((r) => !r)}\n      onKeyDown={(e) => e.key === 'Enter' && setRevealed((r) => !r)}\n      style={{\n        background: `var(${token.name})`,\n        color: textColor,\n        padding: '10px 12px',\n        fontSize: 13,\n        cursor: 'pointer',\n        display: 'flex',\n        alignItems: 'center',\n        justifyContent: 'space-between',\n        gap: 12,\n        // corners rounded per-bar, not via a clipped wrapper — a wrapper with\n        // overflow:hidden would cut off the copy tooltips popping out of it\n        borderTopLeftRadius: isFirst ? 8 : 0,\n        borderTopRightRadius: isFirst ? 8 : 0,\n        borderBottomLeftRadius: isLast ? 8 : 0,\n        borderBottomRightRadius: isLast ? 8 : 0,\n      }}\n    >\n      <span>{stepOf(token.name)}</span>\n      {revealed && (\n        <span style={{ display: 'flex', alignItems: 'center', gap: 8, fontFamily: CODE_FONT }}>\n          <CopyLabel text=\"Hex\" value={token.value} />\n          <CopyIcon color={textColor} />\n          <CopyLabel text=\"RGB\" value={hexToRgb(token.value)} />\n          <CopyIcon color={textColor} />\n          <CopyLabel text=\"Token\" value={token.name} />\n        </span>\n      )}\n    </div>\n  );\n}\n\nfunction ColorRamp({ family, tokens }) {\n  const sorted = useMemo(\n    () => [...tokens].sort((a, b) => stepSortValue(stepOf(a.name)) - stepSortValue(stepOf(b.name))),\n    [tokens],\n  );\n  return (\n    <div>\n      <p style={{ fontSize: 14, fontWeight: 600, marginBottom: 8 }}>{familyDisplayName(family)}</p>\n      <div>\n        {sorted.map((t, i) => (\n          <RampBar key={t.name} token={t} isFirst={i === 0} isLast={i === sorted.length - 1} />\n        ))}\n      </div>\n    </div>\n  );\n}\n\nfunction PrimitiveRamps() {\n  const tokens = useMemo(\n    () => listTokens('--pepper-core-color-').filter((t) => !t.name.includes('opacity')),\n    [],\n  );\n  const families = useMemo(() => groupPrimitives(tokens, '--pepper-core-color-'), [tokens]);\n  const order = useMemo(() => orderFamilies(families), [families]);\n\n  return (\n    <div>\n      <h3 style={{ font: 'var(--pepper-typography-heading-h3)', margin: '0 0 8px' }}>Primitives</h3>\n      <p style={{ font: 'var(--pepper-typography-body-md)', margin: '0 0 24px' }}>\n        Raw palette scales — the building blocks behind every semantic token. Don't reference these\n        directly in product code; use the semantic tokens instead.\n      </p>\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 24 }}>\n        {order.map((family) => (\n          <ColorRamp key={family} family={family} tokens={families.get(family)} />\n        ))}\n      </div>\n    </div>\n  );\n}\n\nfunction semanticGroup(name) {\n  const rest = name.replace('--pepper-color-', '');\n  const segs = rest.split('-');\n  if (segs[0] === 'fg') return `fg-${segs[1]}`; // fg-text, fg-icon, fg-stroke\n  return segs[0]; // bg, component, static, border, etc.\n}\n\nfunction SemanticPalette() {\n  const tokens = useMemo(\n    () =>\n      listTokens('--pepper-color-').concat(listTokens('--pepper-overlay-')).concat(listTokens('--pepper-state-')),\n    [],\n  );\n  const groups = useMemo(() => {\n    const map = new Map();\n    for (const token of tokens) {\n      const key = token.name.startsWith('--pepper-color-')\n        ? semanticGroup(token.name)\n        : token.name.startsWith('--pepper-overlay-')\n          ? 'overlay'\n          : 'state';\n      if (!map.has(key)) map.set(key, []);\n      map.get(key).push(token);\n    }\n    return map;\n  }, [tokens]);\n\n  return (\n    <div>\n      <p>\n        Semantic / component / static / overlay / state tokens (<code>--pepper-*</code>) — use these\n        in product. They reference primitives above and are the ones that flip with the theme toggle\n        in the toolbar.\n      </p>\n      {[...groups.entries()].map(([group, groupTokens]) => (\n        <section key={group} style={{ marginBottom: 24 }}>\n          <h4 style={{ marginBottom: 4 }}>{group}</h4>\n          <TokenGrid tokens={groupTokens} />\n        </section>\n      ))}\n    </div>\n  );\n}\n\nexport default {\n  title: 'Foundations/Color',\n  tags: ['ai-generated'],\n  parameters: { layout: 'padded' },\n};\n\nexport const Semantic = {\n  render: () => <SemanticPalette />,\n};\n\nexport const Primitives = {\n  render: () => <PrimitiveRamps />,\n};\n"
      },
      "stories": [
        {
          "id": "foundations-color--semantic",
          "name": "Semantic",
          "snippet": "const Semantic = () => <SemanticPalette />;"
        },
        {
          "id": "foundations-color--primitives",
          "name": "Primitives",
          "snippet": "const Primitives = () => <PrimitiveRamps />;"
        }
      ]
    },
    "foundations-effects": {
      "id": "foundations-effects",
      "name": "Effects",
      "path": "./src/stories/foundations/Effects.stories.jsx",
      "jsDocTags": {},
      "error": {
        "name": "No component found",
        "message": "We could not detect the component from your story file. Specify meta.component.\n  20 | }\n  21 |\n> 22 | export default {\n     | ^\n  23 |   title: 'Foundations/Effects',\n  24 |   tags: ['ai-generated'],\n  25 |   parameters: { layout: 'padded' },\n\n./src/stories/foundations/Effects.stories.jsx:\nimport { useMemo } from 'react';\nimport { listTokens } from '../../utils/tokens';\n\nconst swatchBase = {\n  width: 96,\n  height: 64,\n  background: 'var(--pepper-color-bg-surface-brand-primary, #0064fa)',\n};\n\nfunction EffectRow({ name, value, style }) {\n  return (\n    <div style={{ display: 'flex', alignItems: 'center', gap: 16, padding: '8px 0' }}>\n      <div style={{ ...swatchBase, ...style }} />\n      <div style={{ fontFamily: 'monospace', fontSize: 12 }}>\n        <div>{name}</div>\n        <div style={{ opacity: 0.6 }}>{value}</div>\n      </div>\n    </div>\n  );\n}\n\nexport default {\n  title: 'Foundations/Effects',\n  tags: ['ai-generated'],\n  parameters: { layout: 'padded' },\n};\n\nexport const BorderRadius = {\n  render: () => {\n    const tokens = listTokens('--pepper-border-radius-');\n    return (\n      <div>\n        {tokens.map((t) => (\n          <EffectRow key={t.name} {...t} style={{ borderRadius: t.value }} />\n        ))}\n      </div>\n    );\n  },\n};\n\nexport const BorderWidth = {\n  render: () => {\n    const tokens = listTokens('--pepper-border-width-');\n    return (\n      <div>\n        {tokens.map((t) => (\n          <EffectRow\n            key={t.name}\n            {...t}\n            style={{ background: 'transparent', border: `${t.value} solid var(--pepper-color-fg-stroke, #333)` }}\n          />\n        ))}\n      </div>\n    );\n  },\n};\n\nexport const Shadow = {\n  render: () => {\n    const tokens = listTokens('--pepper-shadow-').filter((t) => !t.name.includes('offset'));\n    return (\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 32, padding: 16 }}>\n        {tokens.map((t) => (\n          <div key={t.name} style={{ textAlign: 'center' }}>\n            <div\n              style={{\n                ...swatchBase,\n                width: '100%',\n                borderRadius: 8,\n                boxShadow: `var(${t.name})`,\n                margin: '0 auto 12px',\n              }}\n            />\n            <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n          </div>\n        ))}\n      </div>\n    );\n  },\n};\n\nexport const Blur = {\n  render: () => {\n    const tokens = listTokens('--pepper-blur-');\n    return (\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(140px, 1fr))', gap: 24, padding: 16 }}>\n        {tokens.map((t) => (\n          <div key={t.name} style={{ textAlign: 'center' }}>\n            <div\n              style={{\n                ...swatchBase,\n                width: '100%',\n                borderRadius: 8,\n                filter: `var(${t.name})`,\n                margin: '0 auto 12px',\n              }}\n            />\n            <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n          </div>\n        ))}\n      </div>\n    );\n  },\n};\n\nexport const Opacity = {\n  render: () => {\n    const tokens = listTokens('--pepper-core-opacity-values-');\n    return (\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(90px, 1fr))', gap: 12, padding: 16 }}>\n        {tokens.map((t) => (\n          <div key={t.name} style={{ textAlign: 'center' }}>\n            <div\n              style={{\n                width: 56,\n                height: 56,\n                borderRadius: 8,\n                margin: '0 auto 8px',\n                background: 'var(--pepper-core-color-brand-primary-blue-400, #0064fa)',\n                opacity: `var(${t.name})`,\n              }}\n            />\n            <div style={{ fontFamily: 'monospace', fontSize: 11 }}>{t.value}</div>\n          </div>\n        ))}\n      </div>\n    );\n  },\n};\n\nexport const Gradient = {\n  render: () => {\n    const tokens = listTokens('--pepper-gradient-');\n    return (\n      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(220px, 1fr))', gap: 24, padding: 16 }}>\n        {tokens.map((t) => (\n          <div key={t.name} style={{ textAlign: 'center' }}>\n            <div\n              style={{\n                ...swatchBase,\n                width: '100%',\n                height: 100,\n                borderRadius: 8,\n                background: `var(${t.name})`,\n                margin: '0 auto 12px',\n              }}\n            />\n            <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n          </div>\n        ))}\n      </div>\n    );\n  },\n};\n"
      },
      "stories": [
        {
          "id": "foundations-effects--border-radius",
          "name": "Border Radius",
          "snippet": "const BorderRadius = () => {\n  const tokens = listTokens('--pepper-border-radius-');\n  return (\n    <div>\n      {tokens.map((t) => (\n        <EffectRow key={t.name} {...t} style={{ borderRadius: t.value }} />\n      ))}\n    </div>\n  );\n};"
        },
        {
          "id": "foundations-effects--border-width",
          "name": "Border Width",
          "snippet": "const BorderWidth = () => {\n  const tokens = listTokens('--pepper-border-width-');\n  return (\n    <div>\n      {tokens.map((t) => (\n        <EffectRow\n          key={t.name}\n          {...t}\n          style={{ background: 'transparent', border: `${t.value} solid var(--pepper-color-fg-stroke, #333)` }}\n        />\n      ))}\n    </div>\n  );\n};"
        },
        {
          "id": "foundations-effects--shadow",
          "name": "Shadow",
          "snippet": "const Shadow = () => {\n  const tokens = listTokens('--pepper-shadow-').filter((t) => !t.name.includes('offset'));\n  return (\n    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 32, padding: 16 }}>\n      {tokens.map((t) => (\n        <div key={t.name} style={{ textAlign: 'center' }}>\n          <div\n            style={{\n              ...swatchBase,\n              width: '100%',\n              borderRadius: 8,\n              boxShadow: `var(${t.name})`,\n              margin: '0 auto 12px',\n            }}\n          />\n          <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n        </div>\n      ))}\n    </div>\n  );\n};"
        },
        {
          "id": "foundations-effects--blur",
          "name": "Blur",
          "snippet": "const Blur = () => {\n  const tokens = listTokens('--pepper-blur-');\n  return (\n    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(140px, 1fr))', gap: 24, padding: 16 }}>\n      {tokens.map((t) => (\n        <div key={t.name} style={{ textAlign: 'center' }}>\n          <div\n            style={{\n              ...swatchBase,\n              width: '100%',\n              borderRadius: 8,\n              filter: `var(${t.name})`,\n              margin: '0 auto 12px',\n            }}\n          />\n          <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n        </div>\n      ))}\n    </div>\n  );\n};"
        },
        {
          "id": "foundations-effects--opacity",
          "name": "Opacity",
          "snippet": "const Opacity = () => {\n  const tokens = listTokens('--pepper-core-opacity-values-');\n  return (\n    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(90px, 1fr))', gap: 12, padding: 16 }}>\n      {tokens.map((t) => (\n        <div key={t.name} style={{ textAlign: 'center' }}>\n          <div\n            style={{\n              width: 56,\n              height: 56,\n              borderRadius: 8,\n              margin: '0 auto 8px',\n              background: 'var(--pepper-core-color-brand-primary-blue-400, #0064fa)',\n              opacity: `var(${t.name})`,\n            }}\n          />\n          <div style={{ fontFamily: 'monospace', fontSize: 11 }}>{t.value}</div>\n        </div>\n      ))}\n    </div>\n  );\n};"
        },
        {
          "id": "foundations-effects--gradient",
          "name": "Gradient",
          "snippet": "const Gradient = () => {\n  const tokens = listTokens('--pepper-gradient-');\n  return (\n    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(220px, 1fr))', gap: 24, padding: 16 }}>\n      {tokens.map((t) => (\n        <div key={t.name} style={{ textAlign: 'center' }}>\n          <div\n            style={{\n              ...swatchBase,\n              width: '100%',\n              height: 100,\n              borderRadius: 8,\n              background: `var(${t.name})`,\n              margin: '0 auto 12px',\n            }}\n          />\n          <div style={{ fontFamily: 'monospace', fontSize: 12 }}>{t.name}</div>\n        </div>\n      ))}\n    </div>\n  );\n};"
        }
      ]
    },
    "foundations-spacing": {
      "id": "foundations-spacing",
      "name": "Spacing",
      "path": "./src/stories/foundations/Spacing.stories.jsx",
      "jsDocTags": {},
      "error": {
        "name": "No component found",
        "message": "We could not detect the component from your story file. Specify meta.component.\n  19 | }\n  20 |\n> 21 | export default {\n     | ^\n  22 |   title: 'Foundations/Spacing',\n  23 |   tags: ['ai-generated'],\n  24 |   parameters: { layout: 'padded' },\n\n./src/stories/foundations/Spacing.stories.jsx:\nimport { useMemo } from 'react';\nimport { listTokens } from '../../utils/tokens';\n\nfunction SpaceGroup({ prefix, label }) {\n  const tokens = useMemo(() => listTokens(prefix), [prefix]);\n  return (\n    <section style={{ marginBottom: 24 }}>\n      <h4>{label}</h4>\n      {tokens.map(({ name, value }) => (\n        <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 12, padding: '4px 0' }}>\n          <div style={{ width: value, height: 16, background: 'var(--pepper-core-color-brand-primary-blue-400, #0064fa)' }} />\n          <div style={{ fontFamily: 'monospace', fontSize: 12 }}>\n            {name} — {value}\n          </div>\n        </div>\n      ))}\n    </section>\n  );\n}\n\nexport default {\n  title: 'Foundations/Spacing',\n  tags: ['ai-generated'],\n  parameters: { layout: 'padded' },\n};\n\nexport const InsetAndGap = {\n  render: () => (\n    <div>\n      <p>\n        <code>--pepper-space-inset-*</code> (internal padding) and <code>--pepper-space-gap-*</code>\n        (space between elements). Bar width shows the actual token value.\n      </p>\n      <SpaceGroup prefix=\"--pepper-space-inset-\" label=\"Inset (padding)\" />\n      <SpaceGroup prefix=\"--pepper-space-gap-\" label=\"Gap (proximity/stacking)\" />\n    </div>\n  ),\n};\n\nexport const LayoutSpacing = {\n  render: () => (\n    <div>\n      <p>\n        Responsive layout tokens — desktop values shown here. Tablet/mobile overrides live under\n        <code> [data-theme=\"tablet\"|\"mobile\"]</code> in <code>space.css</code>.\n      </p>\n      <SpaceGroup prefix=\"--pepper-space-theme-spacing-\" label=\"Layout spacing (desktop)\" />\n    </div>\n  ),\n};\n"
      },
      "stories": [
        {
          "id": "foundations-spacing--inset-and-gap",
          "name": "Inset And Gap",
          "snippet": "const InsetAndGap = () => (\n  <div>\n    <p>\n      <code>--pepper-space-inset-*</code> (internal padding) and <code>--pepper-space-gap-*</code>\n      (space between elements). Bar width shows the actual token value.\n    </p>\n    <SpaceGroup prefix=\"--pepper-space-inset-\" label=\"Inset (padding)\" />\n    <SpaceGroup prefix=\"--pepper-space-gap-\" label=\"Gap (proximity/stacking)\" />\n  </div>\n);"
        },
        {
          "id": "foundations-spacing--layout-spacing",
          "name": "Layout Spacing",
          "snippet": "const LayoutSpacing = () => (\n  <div>\n    <p>\n      Responsive layout tokens — desktop values shown here. Tablet/mobile overrides live under\n      <code> [data-theme=\"tablet\"|\"mobile\"]</code> in <code>space.css</code>.\n    </p>\n    <SpaceGroup prefix=\"--pepper-space-theme-spacing-\" label=\"Layout spacing (desktop)\" />\n  </div>\n);"
        }
      ]
    },
    "foundations-tokensloaded": {
      "id": "foundations-tokensloaded",
      "name": "TokenProbe",
      "path": "./src/stories/foundations/TokensLoaded.stories.jsx",
      "jsDocTags": {},
      "error": {
        "name": "No component import found",
        "message": "No component file found for the \"TokenProbe\" component.\n  15 | }\n  16 |\n> 17 | export default {\n     | ^\n  18 |   title: 'Foundations/TokensLoaded',\n  19 |   component: TokenProbe,\n  20 |   tags: ['ai-generated'],\n\n./src/stories/foundations/TokensLoaded.stories.jsx:\nimport { expect } from 'storybook/test';\n\n/**\n * Sanity check that pepper-tokens.css actually loaded and resolves to the\n * values declared in tokens/css/base/color.css — not a component test, just\n * a guard so a broken import path fails loudly instead of silently.\n */\nfunction TokenProbe() {\n  return (\n    <div\n      data-testid=\"token-probe\"\n      style={{ background: 'var(--pepper-core-color-brand-primary-blue-400)', width: 40, height: 40 }}\n    />\n  );\n}\n\nexport default {\n  title: 'Foundations/TokensLoaded',\n  component: TokenProbe,\n  tags: ['ai-generated'],\n};\n\nexport const CssCheck = {\n  play: async ({ canvas }) => {\n    const probe = canvas.getByTestId('token-probe');\n    const resolved = getComputedStyle(probe).backgroundColor;\n    // --pepper-core-color-brand-primary-blue-400 is #0064fa in tokens/css/base/color.css\n    await expect(resolved).toBe('rgb(0, 100, 250)');\n  },\n};\n"
      },
      "stories": [
        {
          "id": "foundations-tokensloaded--css-check",
          "name": "Css Check",
          "error": {
            "name": "Error",
            "message": "Could not generate snippet without component name."
          }
        }
      ],
      "import": "import { TokenProbe } from 'pepper-ds-storybook';"
    },
    "foundations-typography": {
      "id": "foundations-typography",
      "name": "Typography",
      "path": "./src/stories/foundations/Typography.stories.jsx",
      "jsDocTags": {},
      "error": {
        "name": "No component found",
        "message": "We could not detect the component from your story file. Specify meta.component.\n  44 | }\n  45 |\n> 46 | export default {\n     | ^\n  47 |   title: 'Foundations/Typography',\n  48 |   tags: ['ai-generated'],\n  49 |   parameters: { layout: 'padded' },\n\n./src/stories/foundations/Typography.stories.jsx:\nimport { useMemo } from 'react';\nimport { listTokens } from '../../utils/tokens';\n\nfunction TypeSample() {\n  const styles = useMemo(() => listTokens('--pepper-typography-'), []);\n\n  return (\n    <div>\n      <p>\n        Composite <code>--pepper-typography-*</code> tokens — each one bundles weight, size,\n        line-height and family from the primitives below into a single <code>font</code> shorthand.\n        Rendered with the real token, not a hardcoded style.\n      </p>\n      {styles.map(({ name }) => (\n        <div key={name} style={{ borderBottom: '1px solid #eee', padding: '16px 0' }}>\n          <div style={{ font: `var(${name})` }}>The quick brown fox jumps over the lazy dog</div>\n          <div style={{ fontFamily: 'monospace', fontSize: 11, opacity: 0.6, marginTop: 4 }}>{name}</div>\n        </div>\n      ))}\n    </div>\n  );\n}\n\nfunction PrimitiveScale({ prefix, label, render }) {\n  const tokens = useMemo(() => listTokens(prefix), [prefix]);\n  return (\n    <section style={{ marginBottom: 24 }}>\n      <h4>{label}</h4>\n      <table style={{ borderCollapse: 'collapse', width: '100%' }}>\n        <tbody>\n          {tokens.map(({ name, value }) => (\n            <tr key={name} style={{ borderBottom: '1px solid #eee' }}>\n              <td style={{ fontFamily: 'monospace', fontSize: 12, padding: '4px 12px 4px 0' }}>{name}</td>\n              <td style={{ fontFamily: 'monospace', fontSize: 12, padding: '4px 12px', opacity: 0.6 }}>\n                {value}\n              </td>\n              <td style={{ padding: '4px 0' }}>{render(value)}</td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </section>\n  );\n}\n\nexport default {\n  title: 'Foundations/Typography',\n  tags: ['ai-generated'],\n  parameters: { layout: 'padded' },\n};\n\nexport const CompositeStyles = {\n  render: () => <TypeSample />,\n};\n\nexport const Primitives = {\n  render: () => (\n    <div>\n      <PrimitiveScale\n        prefix=\"--pepper-font-size-\"\n        label=\"Font size\"\n        render={(v) => <span style={{ fontSize: v }}>Aa</span>}\n      />\n      <PrimitiveScale\n        prefix=\"--pepper-font-weight-\"\n        label=\"Font weight\"\n        render={(v) => <span style={{ fontWeight: v }}>Aa</span>}\n      />\n      <PrimitiveScale\n        prefix=\"--pepper-line-height-\"\n        label=\"Line height\"\n        render={(v) => <span>{v}</span>}\n      />\n      <PrimitiveScale\n        prefix=\"--pepper-core-font-family-\"\n        label=\"Font family\"\n        render={(v) => <span style={{ fontFamily: v }}>Aa Bb Cc</span>}\n      />\n    </div>\n  ),\n};\n"
      },
      "stories": [
        {
          "id": "foundations-typography--composite-styles",
          "name": "Composite Styles",
          "snippet": "const CompositeStyles = () => <TypeSample />;"
        },
        {
          "id": "foundations-typography--primitives",
          "name": "Primitives",
          "snippet": "const Primitives = () => (\n  <div>\n    <PrimitiveScale\n      prefix=\"--pepper-font-size-\"\n      label=\"Font size\"\n      render={(v) => <span style={{ fontSize: v }}>Aa</span>}\n    />\n    <PrimitiveScale\n      prefix=\"--pepper-font-weight-\"\n      label=\"Font weight\"\n      render={(v) => <span style={{ fontWeight: v }}>Aa</span>}\n    />\n    <PrimitiveScale\n      prefix=\"--pepper-line-height-\"\n      label=\"Line height\"\n      render={(v) => <span>{v}</span>}\n    />\n    <PrimitiveScale\n      prefix=\"--pepper-core-font-family-\"\n      label=\"Font family\"\n      render={(v) => <span style={{ fontFamily: v }}>Aa Bb Cc</span>}\n    />\n  </div>\n);"
        }
      ]
    }
  },
  "meta": {
    "docgen": "react-docgen",
    "durationMs": 88
  }
}