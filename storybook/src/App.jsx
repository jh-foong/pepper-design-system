function App() {
  return (
    <div style={{ padding: 40, font: 'var(--pepper-typography-body-md, 16px/1.5 sans-serif)' }}>
      <h1>Pepper DS Storybook pilot</h1>
      <p>
        This Vite app is just the Storybook host — run <code>npm run storybook</code> to see the
        actual foundations pilot (Color, Typography, Spacing, Effects).
      </p>
    </div>
  );
}

export default App;
